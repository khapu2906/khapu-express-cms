const express = require('express');
const to = express.Router();

async function go() {
    
    to.get('/', async (req, res) => {
        return await res.send('<h1>Api Route</h1>');
    })

}

module.exports = { go, to }