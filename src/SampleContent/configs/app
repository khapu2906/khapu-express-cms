// require('dotenv').load();
module.exports = {
    name: process.env.APP_NAME || 'EXPRESS STATION',
    debug: false,
    server: {
        host: process.env.APP_HOST ||'localhost',
        port: process.env.APP_PORT || 4000 
    }
}
process.env["NODE_TLS_REJECT_UNAUTHORIZED"] = 0;