'use strict'
const { Model } = require('spy-84-cms');

class __NAME__ extends Model {

    tableName = ''

    timestamps = true
    
    freezeTableName = true

    createdAt = false

    updatedAt = false

    attributes = {
        id: {
            type: this.dataTypes.INTEGER, //Detail: @link (https://sequelize.org/v5/manual/data-types.html)
            autoIncrement: true,
            primaryKey: true
        }
    }
    
    association = {}

    constructor() {
        super()
    }
    
}   

module.exports = __NAME__.getInstance()
