'use strict'
const { Model, db } = require('spy-84-cms');
// const Comment = require('./Comment')
// const Category = require('./Category')

class Post extends Model {

    tableName = 'post'

    timestamps = true
    
    freezeTableName = true

    createdAt = false

    updatedAt = false

    attributes = {
        id: {
            type: this.dataTypes.INTEGER,
            autoIncrement: true,
            primaryKey: true
        },
        title: {
            type: this.dataTypes.INTEGER,
        },
        content: {
            type: this.dataTypes.TEXT
        }
    }

    constructor(db) {
        super(db)
    }

    // comments() {
    //     this.use.hasMany(Comment.use, {
    //         targeKey: 'id',
    //         foreignKey: 'commentId',
    //         constraints: true
    //     })
    // }

    // categorys() {
    //     this.use.belongsToMany(Category.use, { through: 'PostCategoryAssoc' })
    // }
    
}   

const post = new Post(db);
post.up();
// post.comments();
// post.categorys();

module.exports = post
